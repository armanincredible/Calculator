#include "stack.h"
#include "dora.h"

elem_t calculate (Stack* stack);

int proc (void);

elem_t* make_array (FILE* input_file);

int cmp_files (void);
int make_disasm (void);
int asmr (void);